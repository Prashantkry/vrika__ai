import TextToImage from "./TextToImage";

const HomePage: React.FC = () => {
  return (
    <>
      {/* A website of generating text to image and image to image and image to video */}
      <TextToImage />    
    </>
  );
};

export default HomePage;